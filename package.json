{
  "version": "2.4.2",
  "private": "true",
  "scripts": {
    "bootstrap": "yarn run _tmp_preinstall && yarn install; yarn install && yarn run link:cli",
    "link:cli": "cd packages/cli && yarn link",
    "unlink:cli": "cd packages/cli && yarn unlink",
    "check": "node scripts/check.js",
    "publish": "node scripts/publish.js",
    "test": "yarn run test:cli && yarn run test:client",
    "test:cli": "jest tests/cli/",
    "test:client": "bash tests/playground/test-client.sh",
    "_tmp_preinstall": "mkdir -p packages/cli/build && touch packages/cli/build/index.js"
  },
  "devDependencies": {
    "@types/jest": "^26.0.4",
    "@types/node": "^14.0.23",
    "@types/node-fetch": "^2.5.7",
    "dotenv-cli": "^3.2.0",
    "inquirer": "^7.3.2",
    "jest": "^26.1.0",
    "node-fetch": "^2.6.0",
    "ts-jest": "^26.1.2",
    "typescript": "^3.9.6"
  },
  "workspaces": [
    "packages/*"
  ]
}
